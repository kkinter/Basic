# https://www.acmicpc.net/problem/2579
# f(n) = max(f(n - 1) + floor_score[n])

n = int(input())
floor_score = []
for _ in range(n):
    score = int(input())
    floor_score.append()

dp = [0] * 301
# for idx in range(1, 301):
    
#     dp[idx] = floor_score[idx]
    
#     if dp[idx]
